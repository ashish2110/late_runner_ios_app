# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Late_Runner' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Late_Runner
  pod 'Firebase/Core'
  pod 'FirebaseUI/Auth'
  pod 'Firebase/Database'
  target 'Late_RunnerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Late_RunnerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
